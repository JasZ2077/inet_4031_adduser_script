# inet_4031_adduser_script
# This is a README!
# READ BEFORE USE

## What it Does:
   reads list of users and creates them in a linux system

## How to Run:
   python3 create-users.py < create-users.input
   or ./create-users.py < create-userrs.input
   sudo is not necessary.

## Implementation:
   
   Creates User
   Creates Password
   Assigns Users to their assigned group.
